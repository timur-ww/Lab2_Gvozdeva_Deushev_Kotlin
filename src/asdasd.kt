fun main() {
    /*println("выходите бесы мы станцуем джерси")
    val num = 10
    println(num)*/
    /*println("Введите первое число ")
    val num1 = readln().toInt()
    println("Введите второе число ")
    val num2 = readln().toInt()
    if (num1 > num2) {
        println("Первое число больше ")
    } else {
        println("Второе число больше ")
    }

    val max = if (num1 > num2) num1 else num2
    println("Мксимальное число $max")*/
    /*val age = readln().toInt()
    val status = if (age >= 18) "Совершеннолетний" else "Козявка"
    println(status)*/


    /*val hp: Int = 100
    println("Введите урон: ")
    val damage = readln().toInt()
    println("Щит активен?(да/нет): ")
    val shield = readln().toBoolean()
    if (shield == true){
        hp -= damage / 2
    } else{
        hp -= damage
    }*/


    /* println("Введите ваш уровень: ")
     val lvl = readln().toInt()
     println("Есть ли випка(true/false): ")
     val vip = readln().toBoolean()
     if (lvl >= 30 && vip == true) {
         println("Доступ разрешен")
     } else if (lvl >= 50) {
         println("Доступ разрешен")
     } else {
         println("Доступ запрещен")
     }*/

    // Шаг 8. Введение в коллекции

    /*val daysInMonth = listOf(31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31)

    print("Введите номер месяца (1-12): ")
    val input = readln().toInt()

    if (input in 1..12) {
        val days = daysInMonth[input - 1]
        println("В этом месяце $days дней.")
    }
    else {
        println("Неверный номер месяца. Введите число от 1 до 12.")
    }
    if (input in 1..12) {
        val days = daysInMonth[input - 1]
        println("В этом месяце $days дней.")
    }
    else {
        println("Некорректный номер месяца.")
    }
    val lowerCaseLetter: Char = 'a'
    val upperCaseLetter: Char = 'Q'
    val number: Char = '1'
    val space: Char = ' '
    val dollar: Char = '$'

    val ch = '\u0040'
    println(ch)

    val ch = 'a'
    println(ch.code) // 97
    val num = 97
    println(num.toChar()) // a


    val ch: Char = readln().first()

    val ch1 = 'b'
    val ch2 = ch1 + 1 // 'c'
    val ch3 = ch2 - 2 // 'a'
    val ch = 'A'
    ch += 10
    println(ch) // 'K'
    println(++ch) // 'L'
    println(++ch) // 'M'
    println(--ch) // 'L'

    val one = '1'
    val isDigit = one.isDigit() // true
    val isLetter = one.isLetter() // false

    val capital = 'A'
    val small = 'e'
    val isLetterOnDigit = capital.isLetterOnDigit() // true
    val isUpperCase = capital.isUpperCase() // true
    val isLowerCase = capital.isLowerCase() // false

    val capitalEString = small.uppercase() // "E"
    val capitalE = small.uppercaseChar() // 'E'*/

}